## 2주차 B회차: 데이터 전처리와 특성 공학 — 실습

> **미션**: 제공된 코드를 실행해 결과를 확인한 뒤, AI 코딩 도구를 활용해 코드를 직접 수정·확장하며 전처리의 효과를 체험한다

---

### 실습 방식

이 실습은 **실행 → 이해 → 직접 코딩** 3단계로 진행한다.

1. **실행**: 제공된 코드를 그대로 실행해 결과를 확인한다
2. **이해**: 코드 구조와 결과를 읽고 왜 그런 결과가 나왔는지 파악한다
3. **직접 코딩**: AI 코딩 도구(Copilot, Claude, ChatGPT 등)에 프롬프트를 주어 코드를 수정하거나 새로 작성한다

**제출 형태**: 개별 제출 — 실행 결과 + 직접 작성한 코드 + 해석

**실습 환경 준비**:

```bash
pip install scikit-learn numpy pandas matplotlib
pip install umap-learn            # 실습 3
pip install sentence-transformers  # 실습 4
```

---

### 실습 1: 결측치 처리 방법 비교

#### Step 1 — 실행

`practice/chapter2/code/2-1-missing-values.py`를 실행한다.

```bash
cd practice/chapter2/code
python 2-1-missing-values.py
```

출력에서 아래 표를 채운다.

| 방법 | RMSE | 처리 시간 | 사용 훈련 데이터 수 |
| ---- | ---- | --------- | ------------------- |
| 행 삭제 | | | |
| 평균 대체 | | | |
| 중앙값 대체 | | | |
| KNN 대체 (K=5) | | | |
| 반복 대체 (MICE) | | | |

#### Step 2 — 이해

코드를 열어 핵심 구조를 확인한다.

```python
# 이 파이프라인이 하는 일을 설명할 수 있는가?
pipeline = Pipeline([
    ('imputer', imputer),
    ('scaler', StandardScaler()),
    ('regressor', Ridge(alpha=1.0))
])
```

- 행 삭제 시 훈련 데이터가 원래의 몇 %로 줄었는가?
- 결측 비율 20%에서 8개 변수가 있으면, 행 하나라도 결측이 있을 확률은 약 1-(0.8)⁸ ≈ 83%다. 이것이 행 삭제의 RMSE가 높은 이유다.

#### Step 3 — 직접 코딩

아래 프롬프트를 AI 코딩 도구에 입력해서 코드를 작성하고, 실행 결과를 비교한다.

**프롬프트 1**: 결측 비율 변경 실험

> `2-1-missing-values.py`를 수정해서, missing_rate를 0.1, 0.2, 0.3, 0.5로 바꾸며 각 방법의 RMSE를 비교하는 표를 출력하는 코드를 작성해줘.

결과를 기록한다:

| 결측 비율 | 행 삭제 RMSE | 평균 대체 RMSE | KNN 대체 RMSE |
| --------- | ------------ | -------------- | ------------- |
| 10% | | | |
| 20% | | | |
| 30% | | | |
| 50% | | | |

- 결측 비율이 높아지면 어떤 방법의 성능이 가장 크게 나빠지는가?
- 어떤 방법이 가장 안정적인가?

**프롬프트 2** (선택): 결측 지시 변수 추가

> California Housing 데이터에 20% 결측을 넣고, 평균 대체 + 결측 지시 변수(MissingIndicator)를 추가한 파이프라인으로 Ridge 회귀의 RMSE를 측정하는 코드를 작성해줘. 기존 평균 대체만 한 경우와 비교해줘.

---

### 실습 2: 스케일링과 파이프라인

#### Step 1 — 실행

`practice/chapter2/code/2-2-scaling-pipeline.py`를 실행한다.

```bash
python 2-2-scaling-pipeline.py
```

출력에서 아래 표를 채운다.

**KNN (거리 기반)**:

| 스케일링 방법 | RMSE |
| ------------- | ---- |
| 없음 | |
| StandardScaler | |
| MinMaxScaler | |
| RobustScaler | |
| MaxAbsScaler | |

**Random Forest (트리 기반)**:

| 스케일링 방법 | RMSE |
| ------------- | ---- |
| 없음 | |
| StandardScaler | |
| MinMaxScaler | |

**데이터 누수 시연**:

| 방법 | RMSE |
| ---- | ---- |
| 잘못된 (전체 데이터 스케일링) | |
| 올바른 (훈련 데이터만 스케일링) | |

#### Step 2 — 이해

- KNN에서 "스케일링 없음"과 "StandardScaler"의 RMSE 차이를 확인한다. 왜 이런 차이가 나는가?
- Random Forest에서 RMSE가 거의 같은 이유: 트리 모델은 `MedInc > 3.5` 같은 분할을 하는데, MedInc에 10을 곱해도 `MedInc > 35.0`으로 바뀔 뿐 결과가 동일하다.

#### Step 3 — 직접 코딩

**프롬프트 3**: 이상치가 스케일링에 미치는 영향

> California Housing 데이터에 인위적으로 이상치를 추가하고(상위 1% 값을 100배로 키움), StandardScaler와 RobustScaler로 KNN 성능을 비교하는 코드를 작성해줘. 이상치가 없을 때와 있을 때의 RMSE 변화를 표로 출력해줘.

| 조건 | StandardScaler RMSE | RobustScaler RMSE |
| ---- | ------------------- | ----------------- |
| 이상치 없음 | | |
| 이상치 있음 | | |

- 이상치가 있을 때 어느 스케일러가 더 안정적인가?
- 왜 그런가? (힌트: Standard는 평균/표준편차, Robust는 중앙값/IQR)

**프롬프트 4**: 교차검증과 파이프라인

> California Housing 데이터에서 StandardScaler + KNN 파이프라인을 5-fold 교차검증으로 평가하는 코드를 작성해줘. cross_val_score를 사용하고 평균 RMSE와 표준편차를 출력해줘.

이 코드에서 파이프라인이 각 폴드마다 스케일러를 다시 fit하는지 확인한다.

---

### 실습 3: 차원 축소 비교

#### Step 1 — 실행

`practice/chapter2/code/2-3-dimensionality-reduction.py`를 실행한다.

```bash
python 2-3-dimensionality-reduction.py
```

출력에서 아래 표를 채운다.

| 방법 | 처리 시간 | Silhouette Score |
| ---- | --------- | ---------------- |
| PCA | | |
| t-SNE | | |
| UMAP | | |

| PCA 추가 정보 | 값 |
| -------------- | -- |
| PC1 설명 분산 | |
| PC2 설명 분산 | |
| 90% 설명에 필요한 PC 수 | |
| 95% 설명에 필요한 PC 수 | |

저장된 시각화(`data/output/dimensionality_reduction_comparison.png`)를 열어 세 방법의 군집 분리 상태를 눈으로 비교한다.

#### Step 2 — 이해

- PCA는 왜 빠른가? (선형 연산 = 행렬 곱셈 한 번)
- t-SNE는 왜 느린가? (모든 점 쌍의 유사도 계산 O(n²) + 반복 최적화)
- Silhouette Score가 높다 = 같은 군집은 가깝고, 다른 군집은 멀다
- PCA의 PC1+PC2로 전체 분산의 약 20~30%만 설명됨 → 2차원으로 봤을 때 정보 손실이 크다

#### Step 3 — 직접 코딩

**프롬프트 5**: t-SNE perplexity 실험

> Digits 데이터에서 t-SNE의 perplexity를 5, 15, 30, 50으로 바꾸면서 2차원 시각화를 2x2 서브플롯으로 그리는 코드를 작성해줘. 각 시각화 제목에 perplexity 값과 Silhouette Score를 표시해줘.

결과를 확인한다:

| perplexity | Silhouette Score |
| ---------- | ---------------- |
| 5 | |
| 15 | |
| 30 | |
| 50 | |

- perplexity가 결과에 얼마나 큰 영향을 주는가?
- 어떤 값에서 군집이 가장 잘 분리되는가?

**프롬프트 6**: PCA 전처리 후 분류 성능

> Digits 데이터에서 (1) 원본 64차원, (2) PCA 20차원, (3) PCA 10차원으로 각각 KNN 분류 정확도를 비교하는 코드를 작성해줘. 처리 시간도 함께 출력해줘.

| 입력 차원 | 분류 정확도 | 처리 시간 |
| --------- | ----------- | --------- |
| 64 (원본) | | |
| 20 (PCA) | | |
| 10 (PCA) | | |

- 차원을 줄여도 정확도가 거의 안 떨어지는가?
- 처리 시간은 얼마나 줄었는가?

---

### 실습 4: TF-IDF vs 임베딩 비교

#### Step 1 — 실행

`practice/chapter2/code/2-5-text-embedding.py`를 실행한다.

```bash
python 2-5-text-embedding.py
```

출력에서 아래 표를 채운다.

| 방법 | 정확도 | 벡터 차원 | 처리 시간 |
| ---- | ------ | --------- | --------- |
| TF-IDF | | | |
| SBERT | | | |

분류 리포트에서 가장 잘 분류된 카테고리와 가장 못 분류된 카테고리를 기록한다:

- TF-IDF 최고 F1 카테고리: ________, 최저 F1 카테고리: ________
- SBERT 최고 F1 카테고리: ________, 최저 F1 카테고리: ________

#### Step 2 — 이해

이 실습의 실험 설계를 이해한다:

```python
# 동일한 분류기(로지스틱 회귀) 사용
# → 성능 차이 = 입력 표현(TF-IDF vs SBERT)의 품질 차이
result_tfidf = train_and_evaluate(X_train_tfidf, ..., "TF-IDF")
result_sbert = train_and_evaluate(X_train_sbert, ..., "SBERT")
```

- TF-IDF 차원(5,000) vs SBERT 차원(384): 차원이 약 13배 높아도 TF-IDF가 정확도가 낮다. 왜? → 대부분 0인 희소 벡터라 차원 수 ≠ 정보량
- SBERT는 "야구"와 "홈런"이 가깝다는 것을 표현 가능, TF-IDF는 불가능

#### Step 3 — 직접 코딩

**프롬프트 7**: 코사인 유사도 비교

> 아래 세 문장의 TF-IDF 벡터와 SBERT 임베딩을 각각 구하고, 문장 쌍의 코사인 유사도를 비교하는 코드를 작성해줘.
> - "The movie was great and I enjoyed it"
> - "I really liked this excellent film"
> - "The weather is sunny today"
> TF-IDF와 SBERT에서 각각 어느 문장 쌍이 가장 유사하게 나오는지 출력해줘.

결과를 기록한다:

| 문장 쌍 | TF-IDF 유사도 | SBERT 유사도 |
| -------- | ------------- | ------------ |
| 문장1 - 문장2 | | |
| 문장1 - 문장3 | | |
| 문장2 - 문장3 | | |

- 문장1과 문장2는 의미가 비슷한데, TF-IDF에서도 유사하게 나오는가?
- SBERT에서는 어떤가? 차이의 원인은?

**프롬프트 8**: 샘플 수가 분류 성능에 미치는 영향

> 2-5-text-embedding.py를 수정해서, 훈련 데이터를 100개, 500개, 1000개, 1600개로 바꾸며 TF-IDF와 SBERT의 정확도 변화를 비교하는 코드를 작성해줘.

| 훈련 데이터 수 | TF-IDF 정확도 | SBERT 정확도 |
| -------------- | ------------- | ------------ |
| 100 | | |
| 500 | | |
| 1000 | | |
| 1600 | | |

- 데이터가 적을 때 어떤 방법의 우위가 더 커지는가?
- 왜 그런가? (힌트: SBERT는 사전학습된 지식을 이미 가지고 있다)

---

### 제출 기준

- ✓ 4개 제공 코드를 모두 실행하고 결과 수치를 기록했다
- ✓ Step 3의 프롬프트 중 최소 4개 이상을 AI 도구로 코드를 작성하고 실행했다
- ✓ 직접 작성한 코드와 실행 결과를 포함했다
- ✓ 각 실습의 결과를 왜 그런 결과가 나왔는지 1~2문장으로 해석했다

### 바이브 코딩 팁

- AI 도구에 프롬프트를 줄 때, **데이터와 목표를 구체적으로** 적을수록 좋은 코드가 나온다
- 생성된 코드를 그대로 실행하지 말고, **코드를 읽고 이해한 뒤** 실행한다
- 에러가 나면 에러 메시지를 AI에 다시 붙여넣어 수정을 요청한다
- 결과가 예상과 다르면 왜 다른지 AI에게 물어본다
- **AI가 생성한 코드의 결과도 반드시 검증**한다 — 이것이 A회차에서 배운 "AI 도구 사용 원칙"의 실천이다
